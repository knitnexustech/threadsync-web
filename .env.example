# Environment Variables Template

# Copy this file to .env and fill in your actual values

# ============================================
# SUPABASE CONFIGURATION
# ============================================

# Your Supabase project URL
# Find this in: Supabase Dashboard > Settings > API
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key
# Find this in: Supabase Dashboard > Settings > API > Project API keys > anon public
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Your Supabase service role key (REQUIRED FOR SIGNUP)
# Find this in: Supabase Dashboard > Settings > API > Project API keys > service_role
# ⚠️ WARNING: This key bypasses Row Level Security. Keep it secret!
# ⚠️ In production, move signup to a secure Edge Function instead
VITE_SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================
# NOTES
# ============================================

# 1. Never commit your .env file to version control
# 2. The service role key is needed for signup because it bypasses RLS
# 3. In production, implement signup as a Supabase Edge Function
# 4. Keep your service role key secret - it has admin access to your database
